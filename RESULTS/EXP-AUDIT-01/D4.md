# D4 Continuity Closeout — Evidence (2026-09-20)

Owner: D4-continuity (AUDIT-FIX closeout). Scope: root README + `docs/AGENT_CONTINUITY.md` gaps + this file.
No commit (per mission). Did NOT touch `data/README.md`, `data/HF_MANIFEST.md` (D2-owned), `.gitignore`, or `det_cache/`.

## What was done

1. **Root `README.md`** — appended three sections:
   - GitHub-vs-HF split (what lives in git vs HF dataset/ckpt, fetch pointer to `data/HF_MANIFEST.md`,
     never-in-git list, D2 ownership note).
   - Pins & locks: oracle pin `tracking_cellmot @075fc5f5a52d11077f9dc2b074644618f26939e2`,
     S9 lock `embryo_nested_worst` = **0.4193** official (0.5193 = legacy_harness only),
     Gold HALTED note → `docs/AUDIT-FIX.md`.
   - Agent onboarding (5 steps); smoke flags verified live:
     `scripts/score.py --dry-run` and `scripts/trusted_cv.py --smoke` both exist.
2. **`docs/AGENT_CONTINUITY.md`** — two gap fixes:
   - Onboarding step 3 referenced `scripts/hf_pull.py`, which does not exist → now points at
     `data/HF_MANIFEST.md` manifest commands until the script lands.
   - Dataset repo entry marked intended-only with HF-unreachable TODO (no invented URLs).
3. **HF repo listing** — attempted, FAILED (see Blockers). No URLs invented; intended repos
   documented as TODO in root README.

## HF check (no token echoed, none stored in evidence)

- `huggingface-cli`: not installed on box; `huggingface_hub` python package: not installed.
- `whoami` via HF API (`urllib`, token from `/home/box/.secrets/HF_TOKEN` in memory only):
  **HTTP 401 Unauthorized**. Retried with `/home/box/.cache/huggingface/token` (same byte size): **401**.
- Conclusion: HF unreachable from this box on 2026-09-20 (invalid/expired token and/or no network path).
  Real dataset/ckpt repo paths still unknown → recorded as `<user>/biohub-cell-tracking-rd-data` /
  `<user>/biohub-cell-tracking-rd-ckpts` TODO, pending `huggingface-cli whoami` + repo creation.

## Files changed (uncommitted, `git status` clean apart from pre-existing items)

- `README.md` (append-only, 60 → ~100 lines)
- `docs/AGENT_CONTINUITY.md` (2 gap edits)
- `RESULTS/EXP-AUDIT-01/D4.md` (this file; `run.log` placeholder left untouched)

Pre-existing, not mine: `M .gitignore`, `?? data/`, `?? det_cache/`, `?? RESULTS/EXP-AUDIT-01/`.

## Blockers

1. HF token 401 + no `huggingface-cli` → real HF repo URLs/paths cannot be listed; needs a valid
   `HF_TOKEN` refresh and `pip install huggingface_hub` (or D2 follow-up in `data/HF_MANIFEST.md`).
2. GitHub username for HF repos (`davidliang986` in doc vs `davidlueng986-alt` on GitHub) unconfirmed
   until whoami succeeds.
