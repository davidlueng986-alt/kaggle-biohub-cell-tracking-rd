# AUDIT-FIX A3 — Scorer aggregate (S8, S9, S10, S13)

- Agent: A3-scorer-aggregate, Wave 1
- Scope: `scripts/score.py` aggregate ONLY (T_true / zero-div / weights / dup ids) + evidence under `RESULTS/EXP-AUDIT-00/`. Core match/filter/dedup (A1), division rules (A2), `trusted_cv.py` (A4) NOT touched.
- Gold R&D: HALTED (no new gold EXP chase until Wave 1 gate passes).
- Harness versions: PROTOCOL 1.1 (unchanged), SCORER **1.1.1 → 1.2.0** (aggregate semantics changed; see FILES_CHANGED).

## STATUS

- DONE. All four aggregate IDs fixed in `scripts/score.py` and proven **diff=0 vs the official oracle** on 5 frozen fixtures (`diff_a3.py`, exit 0, `pinned=True` × 5).
- `python3 scripts/score.py --dry-run` → green (legacy toy + geometric smoke untouched).
- `scripts/test_trusted_cv.py` → 4/4 OK (no regression; GTs carry T_true so no NaN starvation — see handoff H1).
- `scripts/test_score.py` → 12/14 pass. The 2 failures are EXPECTED: both assert the old audit-flagged behavior (no-T_true fallback). Left for the Wave-1 merge (test file is A1's concurrent worktree; see BLOCKERS).
- A1's `diff_a1.py` re-run → all OK, exit 0 (no core regression from A3 edits).
- No commit made (per instruction).

## ORACLE_PIN

- Repo: `https://github.com/royerlab/kaggle-cell-tracking-competition`, commit `075fc5f5a52d11077f9dc2b074644618f26939e2` (main 2026-07-17), package `tracking_cellmot 0.1.0`, `tracksdata 0.1.0rc10`, `scipy 1.18.1`, `polars 1.44.2`, path `/tmp/opencode/official`, interpreter `/tmp/opencode/venv-oracle/bin/python`.
- Mirrored functions (read + executed, not copied): `metrics.per_sample_metrics` (adj finite iff edge-J finite AND `n_total > 0`), `metrics.summarise` (adj = weight-avg over finite-adj rows, `w_i = TP+FP+FN`; `division_total == 0` → warn + `division_jaccard = NaN`, `score = adj`), `metrics.evaluate_datasets` (same drop rule), `metrics._jaccard` (0/0 → NaN), `scripts/evaluate.py::_read_estimated_n_total` (GT-geff-only, NaN if absent) + `evaluate_pairs` (per-sample try/except → SKIP crashed samples), `scripts/csv_to_geffs.py::build_graph_from_rows` (every row a node; dup `node_id` → last-occurrence-wins `id_map`).
- Oracle's own test `test_summarise_no_divisions_warns_and_drops_term` asserts the S9 drop rule — the implementation follows the pinned code, not just the audit prose (matters for FINDING F5).

## FINDINGS (all CONFIRMED by repro, all FIXED)

- **S8 CONFIRMED + FIXED** — old `gt.get("T_true", pred.get("T_true"))` let a pred ship `T_true == len(nodes)` and zero the node-count penalty (audit repro `score=1.1`). Fix: `score_single` takes T_true GT-only (`T_true_override` else `gt.get("T_true")`); pred `T_true` never consulted. Missing/invalid T_true → `adjust` returns `(NaN, False)` → sample excluded from the adjusted average (official `per_sample_metrics` NaN rule). Fixture `a3_t_true_fallback` (pred carries exploit `T_true=2`, GT none): old `1.1` (audit repro) → new `adj=NaN score=NaN n_adj=0`, exactly the oracle.
- **S9 CONFIRMED + FIXED** — old `dden==0 → div_j=1.0` white-sent `+0.1` (`score = adj + 0.1`). Fix: `div_j = NaN` + `"No divisions present..."` warning + `score = adj` (official `summarise`/`evaluate_datasets` drop rule), at both `score_single` (per-sample) and `score_samples` (run) level. Fixture `a3_zero_div` (perfect edges, T_true present, no divs): old `1.1` → new `1.0`, exactly the oracle.
- **S10 CONFIRMED + FIXED** — three sub-bugs, one fixture family: (a) `edge_jaccard(0/0)` was `1.0`, now `NaN` (official `_jaccard`); (b) zero-count samples were forced to `w=1`, now weight `0` with `NaN` adj so `score_samples` skips them (official `adj_rows` filter); a zero total weight yields `NaN`, not `0.0`; (c) T_true-missing samples counted at raw Jaccard, now skipped. Run level returns new `n_adj` (rows feeding the average, as in official `summarise`). Fixture `a3_zero_counts` (5 samples incl. empty/empty, empty-pred, empty-GT, no-T_true): old `adj≈0.82 score=0.92` (code-derived) → new `adj=0.5 n_adj=2 score=0.5`, exactly the oracle. Fixture `a3_weights` proves the healthy path is preserved: `w_i=TP+FP+FN` weighting on finite rows gives `(1.0·1+0.525·2)/3 = 0.68333…` bit-identical to the oracle (old code agreed here; only the `+0.1` gift differed).
- **S13 CONFIRMED + FIXED** — old `match_nodes` collapsed duplicate pred ids into one dict entry with matching-order-dependent last-wins, silently dropping a real match and producing one-to-many `gt_to_pred` (`{5:1, 7:1}`). Repro measured pre-fix: dup-id fixture scored `{TP:0,FP:1,FN:1}` vs oracle `{TP:1,FP:0,FN:0}`. Fix (aggregate boundary, core/division untouched): new `_expand_duplicate_ids` namespaces dup ids to unique internal ids in `score_single` before any A1/A2 stage — every row stays matchable, edge refs resolve last-occurrence-wins (official `csv_to_geffs` parity), identity fast-path when ids are unique (zero behavior change for all existing inputs). Post-fix fixture `a3_dup_ids`: `{TP:1,FP:0,FN:0} adj=1.0`, exactly the oracle. Dangling edge refs to unknown ids stay repo-tolerant (official csv path `KeyError`s) — documented divergence, out of scope (validator behavior, not metric).
- **F5 MAJOR — audit internal inconsistency S9 vs C1 headline (needs PM ruling, see NEXT_INPUT N1).** The audit's S9 text ("official drops the term → score=edge only") matches the pinned code, but its C1 headline ("EXP-0053 nested_worst 0.4193 → 0.5193 because fold0 has no divisions") ADDS the `+0.1` the S9 rule forbids. Executed proof: official `summarise` on a division-less split returns `division_jaccard=NaN, score=adj` (its own test asserts this). Hence **0.5193 is NOT official-comparable**; the official-comparable 44b6-fold score is **0.4193 with div dropped (NaN)**. A4's published correction follows the old-harness convention, not the oracle. A3 implements the oracle.
- **F6 — oracle crash-skip discovery (evidence for the 0.5 above).** Pinned `evaluate()` CRASHES (not NaN) on empty-pred+empty-gt (`TypeError` in division matching) and nonempty-pred+empty-gt (`SchemaError` in the division join); official `evaluate_pairs` try/excepts these into SKIP. The repo instead NaN-skips them at aggregation — same aggregate effect via a graceful mechanism. `diff_a3.py` mirrors the skip and asserts skip-parity (skipped ⟺ repo `adj=NaN, w=0`). Empty-pred+nonempty-GT evaluates fine on both sides (`0/0/FN`, `adj=0.0`, included). All three empty-combos are frozen inside `a3_zero_counts` with pins (`skipped: [s1_empty, s4_empty_gt]`).
- **F7 — A4 interface divergence (concurrent worktree observation, not edited).** `scripts/trusted_cv.py` (`_composite_micro`, `fit_best`) now hard-encodes `w==0 → 1` and `micro_div = 1.0 if dden == 0` "to mirror score_samples" (A4.md). After the A3 fix those two lines mirror the OLD scorer and diverge from BOTH fixed `score.py` and official `summarise`; additionally `v["edge"]` can now be `NaN` (T_true-missing GTs) which `num += edge*w` propagates into `NaN` micros unless NaN rows are skipped (official `adj_rows` filter). Handoff H1 below.

## FILES_CHANGED (A3 only)

- `scripts/score.py` — aggregate ONLY: `+math/+warnings` imports; `SCORER_VERSION 1.1.1 → 1.2.0` + A3 pin comment; `edge_jaccard` 0/0→NaN (S10); `adjust` official finite-iff-T_true-and-finite-J (S8/S10); new `_expand_duplicate_ids` (S13); `score_single` GT-only T_true + per-sample div-drop (S8/S9/S13); `score_samples` skip-NaN weighting, NaN-on-empty-weight, div-drop+warn, new `n_adj` (S9/S10). Untouched: `_hungarian`, `_match_group_*`, `match_nodes`, `_filter_pred_edges`, `edge_counts` (legacy path intact), all of `division_counts`, legacy toy path, CSV loaders, CLI.
- Evidence (new): `RESULTS/EXP-AUDIT-00/A3.md` (this file), `RESULTS/EXP-AUDIT-00/diff_a3.py`, `RESULTS/EXP-AUDIT-00/a3_report.json`, `RESULTS/EXP-AUDIT-00/fixtures/a3_{t_true_fallback,zero_div,zero_counts,weights,dup_ids}.json` (each with frozen `oracle` pin).
- Did NOT touch: `scripts/trusted_cv.py` (A4's concurrent edits observed, left alone), `scripts/test_score.py` (A1's concurrent worktree, left alone), match/division code, `.gitignore`, no commits.

## DIFFS (repo vs oracle, `diff_a3.py` exit 0)

| fixture | audit | repo | oracle_live | diffs | pinned |
|---|---|---|---|---|---|
| `a3_t_true_fallback` | S8 | adj NaN div NaN score NaN n_adj 0 | identical | 0 | True |
| `a3_zero_div` | S9 | adj 1.0 div NaN score 1.0 n_adj 1 | identical | 0 | True |
| `a3_zero_counts` | S10+S8 | adj 0.5 div NaN score 0.5 n_adj 2 (skips s1/s4 crash-parity, s2 NaN) | identical | 0 | True |
| `a3_weights` | S10 | adj 0.6833333333333332 div NaN score same n_adj 2 | identical bit-for-bit | 0 | True |
| `a3_dup_ids` | S13 | TP 1 FP 0 FN 0 adj 1.0 score 1.0 | identical | 0 | True |

Pre→post magnitudes (old values: S13 edge counts measured pre-fix; S8/S9 audit repros; S10-weight old value code-derived from the removed branches): `1.1 → NaN` (S8 exploit closed), `1.1 → 1.0` (S9 gift removed), `0.92 → 0.5` (S10 exclusion), `{0,1,1} → {1,0,0}` (S13 map fix).

## NEXT_INPUT

- **N1 (PM ruling required): S9-vs-C1.** A3 implements the code-proven oracle (drop term; 44b6-fold official-comparable score stays **0.4193**, div `NaN`). A4's published `0.5193` follows the old-harness convention the audit's own S9 flags as a bug. B1 must NOT republish EXP-0053 standing as `0.5193`-official; either keep `0.4193 (div dropped, official-comparable)` or explicitly label `0.5193` as old-convention-composite. Wave-1 gate acceptance for C1/S9 needs one convention — recommend oracle (S9).
- **H1 (A4 follow-up, trusted_cv.py):** replace `w==0 → 1` with weight-0 + skip-`NaN`-edge rows (official `adj_rows` filter) in `_composite_micro`/`fit_best`, and replace `micro_div = 1.0 if dden == 0` with div-`NaN` + drop-term scoring to match fixed `score_samples`. Until then trusted_cv micros diverge from the fixed scorer on any division-less fold (exactly the EXP-0053 44b6 shape) and go `NaN` on any T_true-missing GT. Latent today only if GTs lack T_true (current EXP-0003 GTs carry it — `test_trusted_cv` 4/4 still green).
- **Wave-1 merge (whoever merges A1+A3):** update 2 unit tests that encode pre-fix behavior — `test_score.py::TestEdge::test_tp_fn` (asserts `adj=1.0` with no T_true anywhere; official: `NaN`) and `TestAggregate::test_micro_weight` (asserts `2/3` with no T_true; official: `NaN`). Official-correct replacements exist: `test_T_true_penalty` pattern (supply T_true) and fixture `a3_weights` (`0.68333…` with T_true). A3 deliberately did not edit the test file (A1 concurrent owner).
- No new fixtures needed from A3. Division-bearing aggregate proof (score with a live div term) is A2's gate; all A3 fixtures are division-free by design so the div comparison is trivially `0 == 0`.

## EVIDENCE

- `/tmp/opencode/venv-oracle/bin/python RESULTS/EXP-AUDIT-00/diff_a3.py` → 5× `[OK] … 0 diffs pinned=True`, `wrote a3_report.json`, exit 0 (final run post-pin-freeze).
- `python3 scripts/score.py --dry-run` → `dry-run OK (v1.1 faithful scorer; legacy toy + geometric smoke pass)`.
- `python3 scripts/test_score.py` → 12/14 (2 expected aggregate-behavior failures, §NEXT_INPUT).
- `python3 scripts/test_trusted_cv.py` → 4/4 OK. A1 `/tmp/opencode/venv-oracle/bin/python RESULTS/EXP-AUDIT-00/diff_a1.py` → 5× OK + `fuzz {n:3000, seed:7, diffs:0}`, exit 0.
- Probes (oracle venv, `/tmp/opencode/probe_*.py`, not committed): `probe_agg.py` (P1–P8 all hold), `probe_dup2.py` (S13 oracle `{TP:1,FP:0,FN:0}`), `probe_empty.py` (empty-combo crash matrix).
- Pre-fix S13 divergence measured in-tree before editing: repo `{TP:0,FP:1,FN:1}` + one-to-many `g2p={5:1,7:1}` vs oracle `{TP:1,FP:0,FN:0}`.

## BLOCKERS

- B1 (expected, non-blocking for A3): the 2 `test_score.py` failures above — they assert audit-flagged buggy behavior and must be updated at merge, not patched around. A3 acceptance (`diff=0` on aggregate fixtures) is met independently.
- B2 (coordination): shared dirty tree — during this session A1 renamed `fixtures/s*` → `fixtures/a1_s*` (+`A1.md`) and A4 edited `scripts/trusted_cv.py` (+`A4.md`). A3 files (`diff_a3.py`, `a3_report.json`, `fixtures/a3_*`, `A3.md`, `score.py` aggregate hunks) do not overlap either agent's paths; A3's `diff_a1.py` re-run rewrote `diff_report.json` but its content is intact (`fuzz 3000/0` present, 5/5 pins True).
- B3 (decision): N1 (S9-vs-C1 convention) must be resolved before B1 retags EXP-0053 or the Wave-1 gate closes; A3's evidence supports the oracle reading.
- Gold stays HALTED (per AUDIT-FIX.md) — no further action from A3.
