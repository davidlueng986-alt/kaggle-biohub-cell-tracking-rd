# D2 Data pointers — keep data docs under data/, .gitignore exceptions (AUDIT-FIX closeout)

- Agent: D2-data. Date (UTC): 2026-09-20.
- Scope: `.gitignore` + `data/README.md` + `data/HF_MANIFEST.md` + this note ONLY.
  Did NOT move docs to `docs/`. Did NOT touch code, weights, or blobs. No commit
  (orchestrator commits).
- Mission: keep `data/README.md` + `data/HF_MANIFEST.md` under `data/`,
  fix `.gitignore` with `!data/README.md` / `!data/HF_MANIFEST.md` exceptions,
  no invented HF URLs, never commit any token.

## STATUS

- DONE. `.gitignore`: `data/` → `data/*` + `!data/README.md` + `!data/HF_MANIFEST.md`.
  (`data/` alone would exclude the parent dir so negations could never re-include;
  `data/*` ignores contents while allowing the two file exceptions.)
- Both docs already existed under `data/` — kept in place, NOT moved to `docs/`.
  Enriched honestly from actual `ls` of `data/` (6 train sequences, patches,
  weights filenames, subset pointer files). No HF URLs invented — manifest states
  first push pending. Removed the old `export HF_TOKEN="$(cat ...)"` line; auth
  note is now generic (env/secret store at runtime).
- Verified: `git add -n data/README.md data/HF_MANIFEST.md` → both addable;
  blobs (`data/train`, `data/patches/...`, `data/weights/...`,
  `data/sample_submission.csv`, `data/SUBSET_IDS.txt`) still ignored via `data/*`;
  token-pattern grep over the three touched files → clean.

## FILES_CHANGED (uncommitted, for orchestrator to commit)

- `.gitignore` (modified: data-ignore block)
- `data/README.md` (modified: honest local-layout pointer doc)
- `data/HF_MANIFEST.md` (modified: TBD manifest, no invented URLs, no token)
- `RESULTS/EXP-AUDIT-00/D2.md` (this note, new)

## EVIDENCE_PATHS

- `data/README.md`, `data/HF_MANIFEST.md` (tracked-candidate pointer docs)
- `.gitignore` lines 1–4 (exceptions)
- `data/train/` (6 seq dirs), `data/patches/MANIFEST.csv`,
  `data/SUBSET_IDS.txt`, `data/zarr.json` (observed, still ignored)

## BLOCKERS

- None. Ready for orchestrator `git add data/README.md data/HF_MANIFEST.md` + commit.
  Do NOT `git add data/` bare (would add nothing extra today since blobs are
  ignored, but explicit paths are safer).
