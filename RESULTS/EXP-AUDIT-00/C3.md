# AUDIT-FIX C3 — Infra/Docs (I1/I3/I4/I5/I6/I7)

- Agent: C3-infra-docs, Wave 3
- Scope: `.github/workflows/*`, `requirements*`, `docs/AUDIT-FIX.md` stale lines,
  `scripts/download_subset.py` + Kaggle docs, STATE hygiene gaps ONLY.
  Did NOT touch scorer/`trusted_cv` logic, notebook, detect/link. No commit.
- Locks: **0.4193 official, 0.5193 legacy_harness** (PM S9 lock 2026-09-20). Gold HALTED. Wave 4 NOT started. No direction invented.

## STATUS

- DONE. All I-items closed or verified-then-coordinated (I4 numbers/gate-order already landed by C1 — verified, not duplicated).
- Validation: `test_score.py` 14/14, `test_trusted_cv.py` 4/4, `trusted_cv --smoke`
  green (PM-locked `loso_micro` 0.7238688007), `diff_a2` 8/8, `diff_a3` 5/5,
  Kaggle CLI auth live, `smoke.yml` YAML-lints, `download_subset.py` compiles.

## FINDINGS_I

- I1 CONFIRMED (nuance) + FIXED/DOCUMENTED — `download_subset.py` paged with
  `kaggle competitions files --format csv`: the audit's "PyPI上唔存在" claim is
  **stale for this env** — installed Kaggle CLI **2.2.4 supports `--format csv`**
  (verified via `kaggle competitions files --help` + live paginated listing,
  191-file page observed). Fixed the durable parts: absolute
  `/home/box/...` root → repo-relative (`Path(__file__).parent.parent/"data"`,
  `BIOHUB_DATA_DIR` override, same value on canonical VM); `SUBSET_IDS.txt`
  (6 ids, 3×44b6+3×6bba) + `SUBSET_FILES.txt` manifest documented in `docs/AUTH.md`.
- I3 CONFIRMED + FIXED — old `smoke.yml` never `pip install`ed, never ran
  `trusted_cv --smoke`, had no official differential test and no required
  unittest (test_score guarded-skippable). Rewritten: pip install + scipy
  version assert; `test_score.py` + `test_trusted_cv.py -v` REQUIRED;
  `trusted_cv --smoke` REQUIRED; official-diff step (`diff_a2` S4–S7/S11–S12 +
  `diff_a3` S8–S10/S13, fixtures-only, no `--fuzz`) with oracle pinned to
  `tracking_cellmot @075fc5f` + `tracksdata==0.1.0rc10`/`polars==1.44.2`,
  install-guarded (SKIPs with warning when PyPI unreachable; diffs MUST exit 0
  when deps install). Job `timeout-minutes: 15`; required-files list extended
  (harness tests, download_subset, diff scripts).
- I4 VERIFIED (C1-coordinated, no duplicate edit) — `knowledge/STATE.md:28,157,191`
  + `knowledge/RESULTS.md:14,80` cite **0.4193 official only** (zero 0.5193
  standing citations repo-wide outside legacy-harness evidence files);
  gate-order one-liner present via C1 Wave3 (`STATE.md:28`,
  `docs/PROTOCOL.md:72,99,135`: primary = `embryo_nested_worst`, LOSO
  robustness-only — matches `docs/AUDIT-FIX.md:11` product decision, not new
  direction); `decision` enum in `RESULTS.md:20`; `cv_tag` 5-enum in
  `PROTOCOL.md` §2.2 + C1 changelog. C3 made NO STATE edit to avoid clobbering
  C1's in-flight mass edits (worktree shifted mid-task — re-verified by grep).
- I5 FIXED — `scipy>=1.12` kept (system scipy **1.18.1 == oracle 1.18.1**);
  `tracksdata==0.1.0rc10` + `polars==1.44.2` pinned as COMMENTED oracle section
  (PyPI unreachable from some VMs → uncommented pins would break base installs);
  CI installs them explicitly with fallback-skip (see I3). Base
  `pip install -r requirements.txt` semantics unchanged.
- I6 FIXED + LIVE-VERIFIED — merger/new-entrant deadline **2026-09-22**,
  final **2026-09-29 23:59 UTC**, 5/day cap now in `docs/AUDIT-FIX.md` header.
  Live `kaggle competitions list` confirms final deadline 2026-09-29,
  userHasEntered=True, teamCount **3754** (proves cached 3287 stale).
  `docs/COMPETITION.md` staleness (3287 teams, missing merger/cap) FLAGGED but
  NOT edited (outside C3 file scope — PM/C1 call).
- I7 VERIFIED + DOCUMENTED — auth path live: `~/.kaggle/kaggle.json` present
  (chmod 600) + `kaggle competitions list --search biohub` exit 0.
  `download_subset.py` now preflights (env `KAGGLE_USERNAME`+`KAGGLE_KEY` OR
  `kaggle.json` → proceed; else exit 3 with login/token/env guidance);
  both branches tested live. `docs/AUTH.md` gains env-var subsection + KGAT
  Bearer-only insufficiency note + CLI≥2.2 note. No secrets committed
  (edits contain names/placeholders only).

## DOCS

- Stale-line fix (`docs/AUDIT-FIX.md:29`): "Micro includes +0.1·div … (nested_worst
  → ~0.5193)" → S9-lock wording (drop-term; official 0.4193; 0.5193 =
  legacy_harness only). Header gains S9-lock + I6 deadline lines. No other
  AUDIT-FIX lines touched.
- Untracked docs TO COMMIT (listed, NOT committed per mission):
  1. `docs/AUDIT-FIX.md` (modified this task: S9 line + deadlines)
  2. `docs/audits/Biohub_Cell-Tracking_Auto-RD_Merged_Audit_Report.md` (audit source of truth)
  3. `docs/AGENT_CONTINUITY.md` (GH/HF split + onboarding)
  4. `data/README.md` + `data/HF_MANIFEST.md` — exist locally (5 + 12 lines,
     pointer-only) BUT `data/` is gitignored → NOT committable without a
     `.gitignore` exception (out of C3 scope). Mismatch noted: AGENT_CONTINUITY
     calls them "(git)" but they cannot land in git as-is → PM/C1 call
     (move pointers to `docs/` or add `!data/README.md !data/HF_MANIFEST.md`).

## FILES_CHANGED (C3 only)

- `.github/workflows/smoke.yml` — I3 rewrite (10 steps, 15-min timeout).
- `requirements.txt` — I5 commented oracle pins (no install-semantics change).
- `docs/AUDIT-FIX.md` — S9 stale-line fix + S9-lock/deadline header (I6).
- `scripts/download_subset.py` — I1/I7: docstring, repo-relative root +
  `BIOHUB_DATA_DIR`, `check_auth()` preflight (exit 3). Linking/download loop untouched.
- `docs/AUTH.md` — I1/I7: env-auth subsection, KGAT note, CLI≥2.2 + SUBSET manifest notes.
- New evidence: `RESULTS/EXP-AUDIT-00/C3.md` (this file).
- NOT touched: `scripts/score.py`, `scripts/trusted_cv.py`, `scripts/test_*.py`,
  notebook, detect/link, `knowledge/STATE.md` + `docs/PROTOCOL.md` (C1-owned),
  `docs/COMPETITION.md` (out of scope). No commit.

## EVIDENCE_PATHS

- `python3 scripts/test_score.py` → 14 tests OK (2026-09-21).
- `python3 scripts/test_trusted_cv.py` → 4 tests OK.
- `python3 scripts/trusted_cv.py --smoke --out /tmp/c3_smoke_check.json` →
  `loso_micro` **0.7238688007** (PM-locked edge-only, div NaN), 16 s.
- `/tmp/opencode/venv-oracle/bin/python RESULTS/EXP-AUDIT-00/diff_a2.py` →
  8/8 OK exit 0 (fixtures-only; committed `diff_report_a2.json` incl. fuzz key
  restored via `git checkout` after validation rewrote it).
- `.../diff_a3.py` → 5/5 OK pinned=True exit 0 (`a3_report.json` restored likewise).
- `kaggle competitions list --search biohub` → exit 0, deadline 2026-09-29, 3754 teams.
- `python3 scripts/download_subset.py` → `auth OK (env=no, kaggle.json=yes)` +
  live pagination; `HOME=/tmp/c3_noauth` (no creds) → AUTH MISSING + exit 3 guidance.
- `python3 -c yaml.safe_load(smoke.yml)` → YAML OK, 1 job / 10 steps / timeout 15.
- `python3 -m py_compile scripts/download_subset.py` → OK.
- `git status --porcelain`: C3 files show as M (workflows, requirements,
  AUDIT-FIX, download_subset, AUTH) + ?? C3.md; sibling agents' in-flight
  worktree edits (PROTOCOL, STATE, trusted_cv, EXP-00xx) left untouched.

## BLOCKERS

- None for C3. Observations (not blockers): (1) full `trusted_cv --full`
  intentionally not run (68-min class, out of scope); (2) `docs/COMPETITION.md`
  teams 3287→3754 + missing merger/cap needs a PM/C1 edit (out of C3 scope);
  (3) `data/README.md`/`HF_MANIFEST.md` gitignored-vs-"(git)" mismatch needs
  PM/C1 ruling; (4) `knowledge/STATE.md` header "v11 RUNNING" freshness needs
  Kaggle-side truth (C1/PM, not C3); (5) concurrent sibling worktree edits mean
  the committer must review `git diff` per-file before staging C3's five files.
