# AUDIT-FIX C2 — Submit chain + geff/harness honesty (P4 must + P5 must)

- Agent: C2-scripts, Wave 3
- Scope: `scripts/graphs_to_csv.py`, `scripts/geff_to_graph.py`, `scripts/perturb_graph.py`, `scripts/trusted_cv.py` (split/parent only) + this file. Did NOT touch `score.py`, link solvers, tags/PROTOCOL/CI/notebook. No commit.
- PM LOCK: S9 official. Gold HALTED (no retraining/tuning). No Wave 4. No direction invented. P3 ruling honored: NO cross-env bit-exact requirement; scipy preferred when present; honest assign provenance kept; solver costs unchanged.

## STATUS

- DONE. P4 (3/3) + P5 (2/2) fixed with tiny repros; `scripts.test_score` 14/14 + `test_trusted_cv.py` 4/4 green; P3 verified intact (no solver/provenance regression).

## FINDINGS_P3_P4_P5

- P3 VERIFIED INTACT (no change, per ruling) — `baseline_link._assign` still prefers scipy `linear_sum_assignment` for N>60 with pure-python fallback (scipy 1.18.1 present in this env); `link()` still reports honest `assign` (`mixed:…` + `assign_backends` per-pair tracking from B3); cost terms byte-identical (`d if d <= maxd else 1e9`, `dummy = maxd + 1e-9`, dummy-target `0.0`). Tie sets may still differ across backends at identical cost — accepted per P3 ruling (no pinned solver). B3 `gap_link` dup-id `ValueError` + self-edge skip also intact (C2 did not touch `*link*` files).
- P4 CONFIRMED + FIXED (all three) —
  (a) `r10_` collision: `main()` stripped `r10_` then last-write-won (`r10_foo_pred.json` + `foo_pred.json` → one `foo`). Now raises `ValueError` naming both source files whenever a (stripped or plain) name collides. Stripping behavior for non-colliding inputs unchanged.
  (b) Empty dataset: `write_csv` silently emitted zero rows (dataset vanishes, breaking "every test dataset present"). Now raises `ValueError` listing empty dataset(s); empty `datasets` dict (header-only file) also raises. Fail-loud, never silent-drop.
  (c) `--check` dup node ids: ids collapsed into a `set`, so dups passed and edge-ref validation ran on the collapsed set. Now records `duplicate node id` per (dataset, id) into errs (INVALID, exit 1).
- P5 CONFIRMED + FIXED (both) —
  (a) `geff_to_graph.read_geff` built nodes via bare `zip(ids,t,z,y,x)`: ragged arrays silently truncated to the shortest, dropping nodes. Now compares all five lengths first and raises `ValueError` with the length map on mismatch.
  (b) split/parent strip: KEPT (not documented-away). `trusted_cv.load_nodes` dropped `split`/`parent` even though frozen full-video det files (e.g. EXP-0021) and the `dog_detect` CLI schema carry them, and the on-demand `ensure_det` writer discarded them (`_, _`). Both now propagate the keys, so `baseline_link`'s `phased` branch engages in CV exactly when the detector split a component (repro: kept `split=True` → `phased=True`). `perturb_graph.perturb` likewise preserves `split`/`parent` when present. Propagation is if-present-only (`.get`/membership): older frozen files lacking the keys (e.g. EXP-0008 curves) behave exactly as before — no data invented, frozen-cache scores unchanged (all frozen artifacts have `split=False` anyway since `split_size=None`, so `phased` stays off there). Keeping is safe for the frozen cache, hence keep + test per mission preference.

## FILES_CHANGED (C2 only)

- `scripts/graphs_to_csv.py` — P4: `write_csv` empty fail-loud; `check` dup-node-id errs; `main` collision `ValueError`. No schema/column changes.
- `scripts/geff_to_graph.py` — P5: node-array length guard raising `ValueError`. No T_true/edge handling changes.
- `scripts/perturb_graph.py` — P5: preserve `split`/`parent` when present. Jitter/coords/edges/ids/T_true unchanged.
- `scripts/trusted_cv.py` — P5 narrow: on-demand `ensure_det` writes CLI-schema `split`/`parent`; `load_nodes` propagates them if-present. No grid/scoring/aggregation/protocol changes.
- Did NOT touch: `scripts/score.py`, `scripts/*link*` (solver costs + B3 provenance intact), `scripts/dog_detect.py`, tags/PROTOCOL/CI/notebook. No commits.

## EVIDENCE_PATHS

- Tiny repros (inline `python3 - <<EOF`, `sys.path.insert(0,'scripts')`, CPU-only, no data): P4 r10_/plain collision → `ValueError` naming both files; empty graph + empty dict → `ValueError`; normal write → 1 row; dup-id CSV → `['dataset ds1: duplicate node id 1']`, clean file → `[]`. P5 `read_geff` source asserts length guard; `perturb` keeps `split=True/parent=3`, omits keys when absent; `load_nodes` (stubbed `ensure_det`) keeps keys, `BL.link` on kept flags → `phased=True`; old-schema det → keys omitted. (This session; no new fixture files — Gold HALTED.)
- `python3 -m unittest scripts.test_score` → 14/14 OK.
- `python3 scripts/test_trusted_cv.py` → 4/4 OK.
- P3 verify: `_assign` scipy-prefer + `mixed:`/`assign_backends` + cost tokens all asserted present via `inspect` (this session).
- `git diff --stat -- scripts/` (expected, C2 only): `geff_to_graph.py`, `graphs_to_csv.py`, `perturb_graph.py`, `trusted_cv.py`. `*link*` diff empty.

## BLOCKERS

1. None for C2 scope. Residual P3 tie-set divergence across backends (equal-cost, both optimal) is accepted per P3 ruling — pinning a solver is a PM product decision, explicitly not taken.
2. Note: `experiments/*` working-tree modifications present (sibling C1 retag work, e.g. EXP-0060 `tuned_ref`) — not C2's, left untouched.
