# AUDIT-FIX A4 — Trusted CV (C1–C6)

- Agent: A4-trusted-cv, Wave 1
- Scope: `scripts/trusted_cv.py` ONLY (+ evidence under `RESULTS/EXP-AUDIT-00/`). `scripts/score.py` NOT touched (A1 owns S1–S3).
- Gold R&D: HALTED (no new gold EXP chase until Wave 1 gate passes).
- Harness versions: PROTOCOL 1.2, SCORER 1.1.0 (unchanged).

## STATUS

- DONE. All C1–C6 audited, C1/C2/C3/C6 fixed in `scripts/trusted_cv.py`, smoke rerun green, EXP-0053 recalculated.
- `scripts/test_trusted_cv.py`: 4/4 pass (no regression).
- No commit made (per instruction).

## FINDINGS C1–C6

- C1 CONFIRMED + FIXED — `micro_of` (LOSO) and embryo-fold `micro` were edge-only (`sum(edge*w)/sum(w)`), dropping `+0.1*div`. `loso_worst` already used composite `score`, producing the impossible envelope min>mean. `fit_best` was already composite (correct), so HP selections are unaffected — only the reported aggregates were wrong.
- C2 CONFIRMED + FIXED — `DATA_TRAIN`/`GT_DIR`/`EXP_DIR` were absolute checkout paths; canonical runner crashed with `FileNotFoundError` on any other machine even though all artifacts are committed. Now `REPO = dirname(scripts)/..` + `os.path.join(...)`. Full default cache is now repo-relative (`os.path.join(REPO,"det_cache")`); smoke still uses `/tmp/trusted_cv_smoke` (portable).
- C3 CONFIRMED + FIXED — `ensure_det` ran `import zarr` unconditionally, so even fully-frozen manifest hits required zarr. Now lazy: import only inside `if missing:` (on-demand detection branch). Proven: frozen `ensure_det("44b6_0113de3b",99.0)` + `("6bba_05b6850b",98.5)` succeed with `zarr` import blocked.
- C4 CONFIRMED (process, no code change) — grid contains `pct=96`, which entered the search space after eval-sample probe EXP-0039 GO @96 (0.29→0.68). Single-commit harness history (`9830528`) already ships 96 + manifest entries `EXP-0039/EXP-0040 @96`. Retroactively removing 96 would invalidate EXP-0053; recommendation: freeze/document grid provenance going forward, do not silently widen grid on eval signal.
- C5 CONFIRMED (test gap, no prod-code change) — `test_trusted_cv.py` covers only tie-breaks (argmax, fewer-dets, smaller-gate-then-higher-pct, determinism). No leakage or aggregation-equivalence regression. Added as evidence here instead of editing tests (out of A4 touch scope): `_composite_micro` unit checks + smoke canonical check below serve as the regression proof.
- C6 CONFIRMED + FIXED — (a) `fit_best(gt_cache)` unused: kept for API compat, now `gt_cache=None` + docstring (no leakage; fit uses cache only). (b) `frames=range(100)` bare hardcode ×2: now `N_FRAMES=100` + `DEFAULT_FRAMES` with provenance comment (verified `data/train/*.zarr` shape `(100,64,256,256)`). (c) provenance dropped: `loso_table` now keeps `T_true`+`assign`; nested `per_sample` was `edge/score/ec` only, now keeps `edge/raw/div/score/ec/dc/n_det/T_true/assign`; nested fold also stores `micro_edge/micro_div`.

## FILES_CHANGED (A4 only)

- `scripts/trusted_cv.py` — C1 `_composite_micro` + LOSO/nested composite fix (+`w==0→1` quirk to mirror `score_samples`); C2 repo-relative paths; C3 lazy zarr; C6 `N_FRAMES`/`DEFAULT_FRAMES`, `gt_cache=None`, full provenance.
- Did NOT touch: `scripts/score.py`, `scripts/test_score.py` (A1's dirty worktree left alone), no commits.
- Evidence (new): `RESULTS/EXP-AUDIT-00/A4.md` (this file), `RESULTS/EXP-AUDIT-00/A4_corrected_EXP0053.json`, `RESULTS/EXP-AUDIT-00/A4_smoke_fixed.json`.

## MICRO_CHECK (rerun evidence, fixed harness)

- `python3 scripts/test_trusted_cv.py -v` → 4/4 OK.
- `python3 scripts/trusted_cv.py --smoke --out RESULTS/EXP-AUDIT-00/A4_smoke_fixed.json` → 2.6 s, green:
  - `loso_micro` fixed (canonical) = **0.8238688007**; edge-only recompute on same cache = **0.7238688007**; delta **+0.1000** — reproduces audit's `0.7239 / 0.8239` to 4 decimals.
  - Both smoke samples have `dc=0,0,0` → `micro_div=1.0` (44b6 GT divs=0, 6bba_05b6850b GT divs=0), so +0.1 is exact.
  - `loso_worst` = 0.8120683922, `embryo_nested_worst` = 0.8120683922 (now consistent: worst ≤ micro no longer violated in the smoke envelope sense; nested folds 1.0281/0.8121).
  - Provenance keys present in output: `T_true`, `assign`, `div`, `dc`, `raw`, `n_det`.
- Static: `grep home/box scripts/trusted_cv.py` → no hits; `REPO`-relative resolve works from `cwd=/tmp`; frozen hits work with zarr blocked.

## EXP0053_CORRECTED (recalculation, no rescore)

Method: fixed composite formula applied to **stored** EXP-0053 counts (scorer v1.1.0). HPs unchanged (`fit_best` was already composite). Full 6-sample rerun with the current tree would mix in A1 scorer changes, so recalc is the correct correction vehicle while Gold is HALTED.

| metric | stored (buggy) | corrected | why |
|---|---|---|---|
| `loso_micro` | 0.4825902886 | **0.4825902886** (edge 0.4825902886, div 0.0) | dc sum (0,0,4) → div 0.0, numerically unchanged but now formula-honest |
| `loso_worst` | 0.2091686409 | **0.2091686409** | already composite, unchanged |
| `embryo_nested.44b6.micro` | 0.4192839812 | **0.5192839812** (edge 0.4192839812, div 1.0, HP 97.0/7.0) | all 3 holdouts `dc=0,0,0` (GT divs=0) → +0.1 |
| `embryo_nested.6bba.micro` | 0.6135725168 | **0.6135725168** (edge 0.6135725168, div 0.0, HP 97.0/7.0) | GT divs 0/3/1, per-sample div 1.0/0.0/0.0 → dTP=0, dden>0 → div 0.0 |
| `embryo_nested_worst` | 0.4192839812 | **0.5192839812** | = min(0.51928…, 0.61357…) |

Headline: **`embryo_nested_worst` 0.4193 → 0.5193** (audit claim confirmed to 4 decimals: 0.5192839811). Machine file: `A4_corrected_EXP0053.json`.

GT-division grounding (no expensive rescore needed): `44b6_*` GT divs = 0/0/0; `6bba_05b6850b/05db0fb1/062c8d37` GT divs = 0/3/1 (counted from `experiments/EXP-0003/gt/*.json` edge maps). 6bba-fold div is 0.0 regardless of the unknown FP on `05db0fb1@97` because dTP=0 and dden≥4.

## NEXT_INPUT (for B1/gate)

- Wave 1 gate still needs A1–A3 (scorer S/L fixes) + full trusted rebaseline under the corrected harness before any promotion claim. Do NOT republish EXP-0053 as a new BTE yet — the 0.5193 correction is formula-only on old-scorer counts; A1's S1/S2/S3 changes will move edges/divs on rerun.
- B1 tags: EXP-0053 standing stays `trusted` envelope but with corrected `embryo_nested_worst=0.5193`; note the correction source (A4 recalc, not a new run).
- Follow-ups (not A4): freeze HP-grid provenance (C4), add leakage/aggregation regression tests to `test_trusted_cv.py` (C5), derive `frames` from zarr/GT instead of `N_FRAMES=100` if heterogeneous videos ever enter the envelope.

## EVIDENCE

- `RESULTS/EXP-AUDIT-00/A4_smoke_fixed.json` — fixed-harness `--smoke` output (canonical 0.8238688007).
- `RESULTS/EXP-AUDIT-00/A4_corrected_EXP0053.json` — stored vs corrected table + derivation.
- Commands: `python3 scripts/test_trusted_cv.py -v`; `python3 scripts/trusted_cv.py --smoke --out RESULTS/EXP-AUDIT-00/A4_smoke_fixed.json`; `grep -rn "home/box" scripts/trusted_cv.py` (empty); zarr-blocked `ensure_det` frozen-hit check.

## BLOCKERS

- None for A4. Note: full 6-sample `--full` rerun intentionally NOT executed (68-min class + would conflate A1 scorer deltas with the C1 formula fix while Gold is HALTED).
